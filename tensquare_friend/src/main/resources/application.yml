server:
  port: 9010
spring:
  application:
    name: tensquare-friend
  datasource:
    url: jdbc:mysql://192.168.68.129:3306/tensquare_friend?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka/
  instance:
    prefer-ip-address: true
jwt:
  config:
    key: itcast